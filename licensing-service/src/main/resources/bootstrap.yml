spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      uri: http://localhost:9000
server:
  port : 8081
encrypt:
  key: "my-secret-key"
