spring:
  application:
    name: organizationservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      uri: http://localhost:9000
server:
  port : 8082
encrypt:
  key: "my-secret-key"
